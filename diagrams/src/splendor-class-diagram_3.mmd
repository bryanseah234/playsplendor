classDiagram
    direction LR
    class GameController {
        -IGameView gameView
        -IConfigProvider configProvider
        -MoveValidator moveValidator
        -GameRuleValidator gameRuleValidator
        -Game game
        -List~Player~ players
        +GameController(final IGameView gameView, final IConfigProvider configProvider)
        +initializeGame() void
        +startGame() void
        -performUndo() boolean
        -processTurn() void
        -getPlayerMove(final Player player) Move
        -handleTokenLimit(final Player player) void
        -createPlayers(final int playerCount) void
        -executeMove(final Move move, final Player player) void
        -checkNobleVisits(final Player player) void
        -displayGameResults() void
        +getGame() Game
        +getPlayers() List~Player~
        -isUndoInput(final String input) boolean
    }

    class PlayerController {
        -Game game
        -IGameView gameView
        -GameRuleValidator gameRuleValidator
        -MoveValidator moveValidator
        +PlayerController(final Game game, final IGameView gameView, final MoveValidator moveValidator)
        +checkNobleVisits(final Player player) void
        -assignNobleToPlayer(final Player player, final Noble noble) void
        +executeTokenDiscard(final Player player, final Move discardMove) void
        -formatDiscounts(final Map<Gem, Integer> discounts) String
        -validateTokenDiscard(final Player player, final Map<Gem, Integer> tokensToDiscard) void
    }

    class TurnController {
        -Game game
        -IGameView gameView
        +TurnController(final Game game, final IGameView gameView)
        +executeMove(final Move move, final Player player) void
        -executeTakeThreeDifferent(final Move move, final Player player) void
        -executeTakeTwoSame(final Move move, final Player player) void
        -executeReserveCard(final Move move, final Player player) void
        -executeBuyCard(final Move move, final Player player) void
        -executeDiscardTokens(final Move move, final Player player) void
        -findCardToReserve(final Move move, final Board board) Card
        -findAvailableCardById(final Board board, final int cardId) Card
        -findCardToBuy(final Move move, final Player player, final Board board) Card
        -processCardPayment(final Player player, final Board board, final Map<Gem, Integer> effectiveCost) void
        -formatGemMap(final Map<Gem, Integer> gems) String
    }

